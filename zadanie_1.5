/*Задача 5. Объединение и фильтрация списков (std::list)
Тема: операции merge, unique, remove_if.

Задание: Даны два отсортированных списка:

list<int> a = {1, 3, 5, 7};
list<int> b = {2, 3, 6, 7, 8};
Объедините их в один, удалите дубликаты и элементы больше 6.

Результат: На выходе должен получиться список: {1, 2, 3, 5, 6}.

Подсказка: Используйте a.merge(b), затем a.unique(), затем a.remove_if() с условием x > 6.*/

#include <list>
#include <iostream>

int main() {
    setlocale(LC_ALL, "Russian");
    std::list<int> a = { 1, 3, 5, 7 };
    std::list<int> b = { 2, 3, 6, 7, 8 };

    //  Объединяем 
    a.merge(b);

    // Теперь 'b' стал пустым, а 'a' содержит объединённую отсортированную последовательность
    std::cout << "После merge: ";
    for (int x : a) std::cout << x << " ";
    std::cout << "\n";

    //  Удаляем подряд идущие дубликаты
    a.unique();

    std::cout << "После unique: ";
    for (int x : a) std::cout << x << " ";
    std::cout << "\n";

    //  Удаляем элементы больше 6
    a.remove_if([](int x) { return x > 6; });

    std::cout << "Итог (удалены >6): ";
    for (int x : a) std::cout << x << " ";
    std::cout << "\n";

    ret
