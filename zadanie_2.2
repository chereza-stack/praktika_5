/*Задача 2.
Вы - частный детектив, которому доверили расследование наличия состава преступления 
в заимствованиях шуток Данилой Продольным у Джорджа Карлина. 
На вход вам даётся строка X из монолога Джорджа Карлина (длиной не более, чем 10000 символов)
и строка из последнего стэндапа Данилы Продольного Y (также не более 10000 символов).

Необходимо выяснить, является ли Y подстрокой в X На вход в файле input1.txt
подается строка X и в файле input2.txt подается строка Y длины не более 10000,
состоящие только из маленьких букв латинского алфавита.

Необходимо вывести слово yes, если первая строка является подстрокой второй строки, или слово no в противном случае.*/

#include <iostream>
#include <fstream>
#include <string>

int main() {
    std::ifstream fin1("input1.txt");
    std::ifstream fin2("input2.txt");

    std::string X, Y;
    std::getline(fin1, X);
    std::getline(fin2, Y);

    // проверяем, является ли Y подстрокой X
    if (X.find(Y) != std::string::npos)
        std::cout << "yes";
    else
        std::cout << "no";

    return 0;
}
